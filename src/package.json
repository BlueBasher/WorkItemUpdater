{
  "scripts": {
    "build": "tsc -p .",
    "postbuild": "npm run package",
    "package": "tfx extension create",
    "clean": "rimraf ./dist && rimraf ./*.vsix"
  },
  "devDependencies": {
    "grunt": "^1.0.0",
    "grunt-exec": "*",
    "rimraf": "^2.5.4",
    "tfx-cli": "^0.3.45"
  },
  "name": "",
  "private": true,
  "version": "0.0.0"
}
